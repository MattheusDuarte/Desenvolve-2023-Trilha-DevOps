apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teste-aluno.fullname" . }}-test-connection"
  labels:
    {{- include "teste-aluno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teste-aluno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
